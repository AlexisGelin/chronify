import 'package:flutter/material.dart';

class CreateAuthor extends StatelessWidget {
  const CreateAuthor({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Tom Budin Remix',
        style: TextStyle(
            // color: Colors.grey[800],
            fontFamily: 'Lexend',
            fontSize: 16));
  }
}
